{{#each posts as |post|}}
    <div class='col-md-5'>
      <p>
        {{post.title}}
      </p>
    </div>
    <div class='col-md-7'>
      <p>
        {{post.body}}
      </p>
    </div>
  {{/each}}